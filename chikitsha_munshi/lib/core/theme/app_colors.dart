import 'package:flutter/material.dart';

class AppColors {
  // 🔵 Primary Buttons, Icons, Highlights
  static const Color primaryLight = Color(0xFF007BFF); // Electric Blue
  static const Color primaryDark = Color(0xFF3399FF);  // Lighter blue for dark mode

  // 🟢 CTA Buttons, Success Indicators
  static const Color successLight = Color(0xFFC7F464); // Lime Green
  static const Color successDark = Color(0xFFE0FF76);  // Slightly brighter for dark

  // ⚫ Background
  static const Color backgroundLight = Color(0xFFF9F9F9); // Soft White
  static const Color backgroundDark = Color(0xFF121212); // True Dark

  // ⚪ Headers, Navbar Background
  static const Color headerLight = Color(0xFF0D1B2A); // Deep Navy
  static const Color headerDark = Color(0xFFEAEAEA);  // Light Text on Dark

  // ⚙️ Secondary Text, Input Borders (Cool Gray)
  static const Color grayLight = Color(0xFF101010);  // Correct Cool Gray
  static const Color grayDark = Color(0xFFFFFFFF);   // Softer for dark mode

  // 🟥 Error Colors
  static const Color errorLight = Color(0xFFFF4C4C); // Bright red
  static const Color errorDark = Color(0xFFFF6B6B);  // Softer in dark mode

  // 🟧 Warning Colors
  static const Color warningLight = Color(0xFFFFA500); // Orange
  static const Color warningDark = Color(0xFFFFB347);  // Lighter orange

  // 🟨 Info / Hint Text
  static const Color infoLight = Color(0xFF4C6EF5);  // Indigo Blue
  static const Color infoDark = Color(0xFF7D9EFF);   // Softer info

  // ⬛ Divider or Border
  static const Color dividerLight = Color(0xFFE0E0E0);
  static const Color dividerDark = Color(0xFF2E2E2E);

  // 🟫 Disabled
  static const Color disabledLight = Color(0xFFBDBDBD);
  static const Color disabledDark = Color(0xFF555555);

  // ✳️ Overlay (for modals, dialogs)
  static const Color overlayLight = Color(0x66000000); // 40% black
  static const Color overlayDark = Color(0x99FFFFFF);  // 60% white

  // 🖍️ Selection Highlight
  static const Color selectionLight = Color(0xFFB3D4FC);
  static const Color selectionDark = Color(0xFF365F9C);
}
